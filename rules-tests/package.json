{
  "name": "freebnb-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Emulator-backed regression tests for firestore.rules.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
